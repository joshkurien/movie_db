require 'spec_helper'

describe MoviesController do

	context "POST create" do
    it "creates movie entry" 
  end

  context "GET new" do
    it "Displays new form for adding movies"
  end

  context "GET edit" do
    it "Fetches spcified movie in an editable form"
  end

  context "PUT update" do
    it "Updates an movie"
  end

  context "DELETE delete" do
    it "Delete an movie"
  end

  context "GET index" do
    it "lists all movies"
  end

  context "GET show" do
    it "shows the details of a movie"
  end

end
